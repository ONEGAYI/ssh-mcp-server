# 大文件扩展实施票据

状态：2026-09-13 #6–#21（含 #18）全部 16 张票据（15 实施票 + 1 终验票）已实施并合入 `feat/large-file-spec`（PR #22，含审查轮 1 修复与复核收尾）。内网离线现场人工验收保留未完成；后半票据（#15 起）的缺陷审查轮按用户指示另行安排。

规格：[本地规格](large-file-spec.md) / [GitHub #5](https://github.com/ONEGAYI/ssh-mcp-server/issues/5)。既有需求 #3 保持开放，远端 Windows 另见 #4。

## 依赖顺序

| 票据 | 交付 | Blocked by |
|---|---|---|
| [#6](https://github.com/ONEGAYI/ssh-mcp-server/issues/6) | refactor: 分离文件快照与提交边界，保持首版行为 | 无 |
| [#7](https://github.com/ONEGAYI/ssh-mcp-server/issues/7) | feat: 登记后执行协议与旧请求拒绝机制 | #6 |
| [#8](https://github.com/ONEGAYI/ssh-mcp-server/issues/8) | feat: 管理临时资源并预留工作区空间 | #6 |
| [#9](https://github.com/ONEGAYI/ssh-mcp-server/issues/9) | feat: 流式读取大文件与元数据版本凭据 | #6 |
| [#10](https://github.com/ONEGAYI/ssh-mcp-server/issues/10) | feat: 大文件精确替换与显式版本覆盖 | #8, #9 |
| [#11](https://github.com/ONEGAYI/ssh-mcp-server/issues/11) | feat: 远端多后端内容搜索与可续扫分页 | #6 |
| [#12](https://github.com/ONEGAYI/ssh-mcp-server/issues/12) | feat: 对齐文件名查找的后端与过滤规则 | #11 |
| [#13](https://github.com/ONEGAYI/ssh-mcp-server/issues/13) | feat: 可校验续传的流式上传事务 | #7, #8, #10 |
| [#14](https://github.com/ONEGAYI/ssh-mcp-server/issues/14) | feat: 可校验续传的流式下载事务 | #13 |
| [#15](https://github.com/ONEGAYI/ssh-mcp-server/issues/15) | feat: 后台传输完成回传、恢复与主动取消 | #13, #14 |
| [#16](https://github.com/ONEGAYI/ssh-mcp-server/issues/16) | feat: 任务与传输结果到期回收且拒绝旧请求 | #7, #8, #15 |
| [#17](https://github.com/ONEGAYI/ssh-mcp-server/issues/17) | feat: 回收过期读取凭据、旧 helper 与遗留临时文件 | #8, #9, #16 |
| [#18](https://github.com/ONEGAYI/ssh-mcp-server/issues/18) | feat: 已有绑定增量配置与策略自动生效 | 无 |
| [#19](https://github.com/ONEGAYI/ssh-mcp-server/issues/19) | feat: 按需返回工作区空间汇总 | #8, #16, #18 |
| [#20](https://github.com/ONEGAYI/ssh-mcp-server/issues/20) | refactor: 移除四项文件管理工具并收缩旧协议 | #10, #12, #15, #17, #18 |
| [#21](https://github.com/ONEGAYI/ssh-mcp-server/issues/21) | test: 验收 200 MiB 文件、续传和清理的完整流程 | #11, #12, #15, #17, #19, #20 |

无前置依赖的 #6、#18 已标 ready-for-agent。其他票解除依赖后再开始；有标签不代表已实现或用户已验收。准备性重构单独成票，功能票均包含行为测试，最后一票只负责跨功能验证，不替代各票测试。

## 票据内容快照

### #6 refactor: 分离文件快照与提交边界，保持首版行为

规格：#5（第 3 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: 无

## 交付范围

- 在现有 FileService/remote files 中分离版本观察、内容读取、替换与提交，暂不改变 16 MiB 和授权语义。
- 通过现有 MCP 与 Python helper 测试验证首版行为，禁止混入大文件新语义。

## 验收条件

- [ ] 现有文件工具行为与错误码保持
- [ ] 已有测试通过
- [ ] 后续实现能独立替换读取和版本观察
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #7 feat: 登记后执行协议与旧请求拒绝机制

规格：#5（第 6.1、9 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #6

## 交付范围

- 新增版本化登记后执行流程，任务执行只接受已持久化登记标识；缺记录拒绝。
- 提供旧任务观察兼容与排空切换门槛，本票不删除旧结果。

## 验收条件

- [ ] 重复 start 只执行一次
- [ ] 响应丢失不会产生第二任务
- [ ] 删除记录后旧标识无法重跑
- [ ] 旧客户端不能绕过新协议
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #8 feat: 管理临时资源并预留工作区空间

规格：#5（第 7.2 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #6

## 交付范围

- 建立两端资源登记、固定锁槽和跨进程空间账本，接入一次真实文件临时提交。
- 每端每工作区默认 10 GiB；目录旁临时文件计入；并发预留不双算。

## 验收条件

- [ ] 并发操作不会超额预留
- [ ] ENOSPC 有明确失败
- [ ] PID 重用不误判
- [ ] 未排空旧写入时拒绝锁协议切换
- [ ] 正式文件提交后移出计量
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #9 feat: 流式读取大文件与元数据版本凭据

规格：#5（第 4.1、4.2 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #6

## 交付范围

- 取消读取体积限制及全文哈希，支持 metadataOnly、按行扫描与字节游标。
- 只交付范围授予凭据；UTF-8、CRLF、超长行有界。

## 验收条件

- [ ] 200 MiB 首中末读取成功
- [ ] 外部变化拒绝旧游标/凭据
- [ ] 截断不授予未交付内容
- [ ] 峰值内存符合规格
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #10 feat: 大文件精确替换与显式版本覆盖

规格：#5（第 4.3 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #8, #9

## 交付范围

- 跨块全文唯一匹配，重复要求扩大上下文；流式同目录原子提交。
- 整体覆盖无需旧全文读取，但必须显式声明并绑定 metadataOnly 版本。

## 验收条件

- [ ] 零/多匹配无写入
- [ ] 扩大原文但未完整读到时拒绝
- [ ] 多项失败不部分提交
- [ ] 创建冲突和外部变化拒绝
- [ ] 权限/BOM/CRLF 保留
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #11 feat: 远端多后端内容搜索与可续扫分页

规格：#5（第 5 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #6

## 交付范围

- Linux rg→GNU grep→Python，统一字面量语义、忽略与隐藏规则。
- 远端有界扫描、游标续扫；不跳过超过 16 MiB 的文件。

## 验收条件

- [ ] 三个后端结果一致
- [ ] 200 MiB 可搜索
- [ ] 跨块/长行不漏命中
- [ ] 预算耗尽返回 partial 与游标
- [ ] 下一页不重扫已完成文件
- [ ] 无匹配与后端错误区分
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #12 feat: 对齐文件名查找的后端与过滤规则

规格：#5（第 5.1 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #11

## 交付范围

- find 采用 rg 枚举或 Python 遍历，复用公共过滤器与分页。
- 保持现有 glob 契约，不使用 grep 模拟文件枚举。

## 验收条件

- [ ] rg 与 Python 的 glob/隐藏/嵌套忽略结果一致
- [ ] .git 内部始终排除
- [ ] 无界枚举被预算阻止且明确报告
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #13 feat: 可校验续传的流式上传事务

规格：#5（第 6.1、6.2 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #7, #8, #10

## 交付范围

- 实现 upload start/status/resume，二进制有界块传输，落盘后确认偏移。
- 两端 transfers 记录、同目录临时文件、分块验证与最终两端 SHA-256。

## 验收条件

- [ ] 200 MiB 上传成功
- [ ] 半块断线恢复只补未确认数据
- [ ] 源变化拒绝
- [ ] 最终校验不重传全文
- [ ] 提交响应丢失核对原事务，不重复覆盖
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #14 feat: 可校验续传的流式下载事务

规格：#5（第 6.1、6.2 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #13

## 交付范围

- 复用传输事务支持反向下载，保留本机允许路径与覆盖版本检查。
- 移除下载每块全文 snapshot 路径，下载不授予模型已读范围。

## 验收条件

- [ ] 200 MiB 下载摘要一致
- [ ] 断线/本机进程退出后恢复
- [ ] 本机目标变化拒绝覆盖
- [ ] 无正式半文件
- [ ] 每块不再全文读取
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #15 feat: 后台传输完成回传、恢复与主动取消

规格：#5（第 6.3、7.1 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #13, #14

## 交付范围

- 扩展 job CLI transfer 子命令及 ZCode 恢复钩子，跟进同会话原传输。
- 取消需确认停止再删未提交数据；提交并发先核对结果；ack 与状态查询分开。

## 验收条件

- [ ] 真实后台完成能回到原对话
- [ ] MCP/本机重启可重挂原传输
- [ ] 不持续投递块进度
- [ ] 取消释放临时数据
- [ ] 已提交目标不回滚
- [ ] 未知不误报成功
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #16 feat: 任务与传输结果到期回收且拒绝旧请求

规格：#5（第 7.1、7.2 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #7, #8, #15

## 交付范围

- 实现懒清理与每小时在线维护，带互斥、游标和每轮预算。
- 任务/传输结果、日志、unknown 记录按规格期限回收；任务日志接入额度并明确截断。

## 验收条件

- [ ] 加速时钟覆盖 3/30 天
- [ ] unknown 到期结果诊断恢复记录确实删除
- [ ] 旧请求不能重跑
- [ ] 查询不续期
- [ ] 离线重连补清理
- [ ] 满额不无限写日志
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #17 feat: 回收过期读取凭据、旧 helper 与遗留临时文件

规格：#5（第 7 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #8, #9, #16

## 交付范围

- 凭据 3 天失效且不复活已读范围；helper 保留当前及活动依赖。
- 崩溃遗留按资源归属/对象/占用核实回收，未知占用仅留最小管理信息。

## 验收条件

- [ ] 过期凭据编辑拒绝且仅需重读片段
- [ ] 活动 helper/临时文件不删
- [ ] 未知结果正文不藏入资源账本
- [ ] 无归属文件不删
- [ ] 普通临时文件不多留三天
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #18 feat: 已有绑定增量配置与策略自动生效

规格：#5（第 8、7.1 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: 无

## 交付范围

- inspect 返回脱敏配置与 revision；update 仅改指定字段并检查冲突。
- 普通 policy 下一操作生效；身份相关字段要求新绑定；现有期限不追溯。

## 验收条件

- [ ] 认证不泄露且保留
- [ ] 并发更新拒绝
- [ ] 无半写文件
- [ ] 无需重启读取新策略
- [ ] 旧 expiresAt 不变
- [ ] 身份变更不能使旧状态失联
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #19 feat: 按需返回工作区空间汇总

规格：#5（第 7.2 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #8, #16, #18

## 交付范围

- remote_workspace 增加 includeStorage=false，返回两端用量、预留与有界清理摘要。
- 默认不追加统计，不新增 MCP 工具，不让维护调用模型。

## 验收条件

- [ ] 默认返回不含统计
- [ ] 开启结果不超过 4 KiB
- [ ] 断线明确 unknown
- [ ] 计量包含目标旁临时文件
- [ ] 记录 schema/样例 token 测量方法
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #20 refactor: 移除四项文件管理工具并收缩旧协议

规格：#5（第 9 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #10, #12, #15, #17, #18

## 交付范围

- 移除 remote_move/delete/mkdir/rmdir 注册和专用调用，文档引导远端 Shell。
- 完成旧调用迁移后移除全文快照传输路径；同步公开契约、README、usage 和离线说明。

## 验收条件

- [ ] 工具列表不再含四工具
- [ ] 其余文件保护保留
- [ ] 旧状态可观察且旧创建入口被拒绝
- [ ] 活动旧客户端阻止切换
- [ ] 不关闭父 issue #3
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

### #21 test: 验收 200 MiB 文件、续传和清理的完整流程

规格：#5（第 10 节）。关联需求：#3。远端 Windows 不在本票范围，另见 #4。

Blocked by: #11, #12, #15, #17, #19, #20

## 交付范围

- 执行跨功能真实 CentOS 7 SSH 与本机 ZCode 验收，记录内存、网络、故障窗口和输出成本。
- 各功能票已自带测试，本票负责整体兼容与离线交付证据。

## 验收条件

- [ ] 20/200 MiB 内存对比满足规格
- [ ] 局部操作无全文网络传输
- [ ] 上传下载故障恢复与最终摘要通过
- [ ] build/Node/Python 套件通过
- [ ] 现场未测明确保留未验收
- [ ] 通过现有 MCP、CLI 或正式 helper 的公共入口先写失败行为测试，再实现；记录验证命令与结果。
- [ ] 更新受影响的契约及使用说明；未完成或未实测的能力不得标为已交付。

## 实施边界

本票贯穿所需接口、服务、远端处理和测试，不单独交付不可使用的一层。沿现有工程结构实施，避免无关重构。默认 CentOS 7 / Python 3.6 标准库、无 root、离线环境。不得关闭或改写父需求 #3、规格 #5。

